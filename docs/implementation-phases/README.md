# MonaCode implementation-plan index

This directory is an index. It does not contain the current normative product implementation plan.

## Preserved G4-R planning history

[`history/g4-r-draft/`](history/g4-r-draft/) is the byte-preserved, non-normative G4-R planning draft. Its 24 Markdown files are fixed by [`history/g4-r-draft/SHA256SUMS`](history/g4-r-draft/SHA256SUMS).

The historical draft records prior planning work only. It does not supersede the adopted G4-R contract and is not product implementation evidence.

## Current adopted G6-R plan

The complete normative G6-R implementation plan is under [`../contracts/monaco-editor-0.56.0/g6-r/implementation-plan/`](../contracts/monaco-editor-0.56.0/g6-r/implementation-plan/).

G6-R was adopted on 2026-08-15 as `G6-R-execution-ready-final`. Its archive verifier, plan verifier, three-round adversarial review, and cold-checkout preflight define the execution-ready start gate. This adoption does not claim product implementation or release acceptance. The frozen parent remains independently verifiable at [`../contracts/monaco-editor-0.56.0/g5-r/`](../contracts/monaco-editor-0.56.0/g5-r/).
