# MonaCode G6-R execution-readiness contract candidate

Status: candidate.
Parent: `G5-R-full-scope-final`.
Plan: not-authored.
Implementation: not-started.
Release acceptance: not-passed.

This directory is the unadopted G6-R skeleton. It embeds the adopted G5-R archive byte-for-byte under `artifacts/parent/g5-r/` as the immutable parent snapshot for the G6-R execution-readiness plan. No G6-R contract, plan, or adoption record exists yet; this skeleton only proves the parent bytes are reproducible and that the embedded G5-R verifier still passes from the embedded root.

## Parent snapshot

`artifacts/parent/g5-r/` is a byte-for-byte copy of `docs/contracts/monaco-editor-0.56.0/g5-r/` (148 files, 4,050,132 bytes, all Git mode 100644, 144 SHA256SUMS rows). The copy is generated by `Tools/G6PlanAuthoring/lib/skeleton.mjs`, which copies with `fs.copyFileSync`, rejects symlinks and special files, confines every destination beneath the supplied target, sets every destination non-executable, hashes source and destination independently, compares the generated path set with `Tools/G6PlanAuthoring/parent-snapshot-paths.txt`, and sorts rows bytewise by source path.

## Verification

```sh
node docs/contracts/monaco-editor-0.56.0/g6-r/artifacts/parent/g5-r/verify-contract.mjs
```

The embedded verifier exits 0 with `adoptedRevision=G5-R-full-scope-final` and `artifactHashesVerified=144`. This confirms the embedded parent bytes remain intact and self-verifying.
